import styled from "styled-components";
import { HeaderWrapper } from "../../styles/carbonComponents.js";
import { WidthWrapper } from "../../styles/styles.js";

export const BigHeaderWrapper = styled(HeaderWrapper)`
    display: block;
    height: 33.5rem;
    padding: 2rem 0;
`;

export const BigHeaderImage = styled.img`
    height: 6.4375rem;
    display: block;
`;

export const BigHeaderWidthWrapper = styled(WidthWrapper)`
    display: block;
    padding: 0 3.5rem;
`;
