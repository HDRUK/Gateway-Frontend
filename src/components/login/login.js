import { Font, StyledButton } from "../../styles/styles.js";
import React from "react";

const Login = () => (
    <StyledButton>
        <Font>Log in</Font>
    </StyledButton>
);

export default Login;
