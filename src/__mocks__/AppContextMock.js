const context = {
    newsItems: {
        test: {
            image: "",
            description: "Test",
            readMore: "http://localhost:3000"
        }
    },
    state: {
        counter: 0
    },
    counterFunc: jest.fn()
};

export default context;
